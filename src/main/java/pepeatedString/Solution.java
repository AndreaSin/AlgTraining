package pepeatedString;
import java.io.*;
import java.math.*;
import java.nio.file.Files;
import java.nio.file.Paths;
import java.security.*;
import java.text.*;
import java.util.*;
import java.util.concurrent.*;
import java.util.function.*;
import java.util.regex.*;
import java.util.stream.*;
import static java.util.stream.Collectors.joining;
import static java.util.stream.Collectors.toList;



public class Solution {
    public static void main(String[] args) throws IOException {
    	
    	
    	
//    	List<String> a = Arrays.asList("asd", "dsa", "2"))
//				
//    			.collect(Collectors.toList());
    	
    	
    	/*
    	List<Integer> bob = Arrays.asList(5, 6, 7);
    	List<Integer> lisa = Arrays.asList(3, 6, 10);
    	int cbob=0;
    	int clisa=0;
    	for (int i = 0; i < bob.size(); i++) {
    		if(bob.get(i) > lisa.get(i)) {
    			cbob++;
    		} else if (bob.get(i) < lisa.get(i)) {
    			clisa++;
    		} 
		}
    	lisa.forEach(null);
    	List<Integer> result = Arrays.asList(clisa,cbob);
    	System.out.println(result);
        */
        
    }
}